import type { ReferenceType, FloatingNodeType, FloatingEvents } from "../types.js";
/**
 * Stores and manages floating elements in a tree structure.
 * This is a backing store for the `FloatingTree` component.
 */
export declare class FloatingTreeStore<RT extends ReferenceType = ReferenceType> {
  readonly nodesRef: React.RefObject<Array<FloatingNodeType<RT>>>;
  readonly events: FloatingEvents;
  addNode(node: FloatingNodeType<RT>): void;
  removeNode(node: FloatingNodeType<RT>): void;
}